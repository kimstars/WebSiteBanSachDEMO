@using WebSiteBanSach.Models;
@model List<GioHang>
<link href="~/Content/TrangChuCss.css" rel="stylesheet" />
@{
    ViewBag.Title = "Sửa Giỏ hàng";
    Layout = "~/Views/LayOut/NguoiDungLayout.cshtml";
}
<div id="divGioHang">
    <table border="1" cellpadding="2" cellspacing="15" width="1000" height="400">
        <tr>
            <td>Tên sách</td>
            <td>Ảnh bìa</td>
            <td id="TenCot">Đơn giá</td>
            <td style="text-align:center;">Số lượng</td>
            <td id="TenCot">Thành tiền</td>
            <td id="TenCot">Cập nhật</td>
            <td id="TenCot">Xóa</td>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item._TenSach</td>
                <td><a href="@Url.Action("XemChiTiet", "Sach", new { @_MaSach=item._MaSach})"><img src="@Url.Content("~/HinhAnhSP/"+@item._AnhBia)" width="100" height="150" /></a></td>
                <td id="ChuSo">@string.Format("{0:0,0}", @item._DonGia) vnđ</td>
                @using (Html.BeginForm("CapNhatGioHang", "GioHang", new { @__MaSach = item._MaSach }))
                {

                    <td id="ChuSo">@Html.TextBox("txtSoLuong", @item._SoLuong)</td>
                    <td id="ChuSo">@string.Format("{0:0,0}", @item._ThanhTien) vnđ</td>
                    <td id="TenCot">
                        <input type="submit" style="background-color:green;color:white;cursor:pointer" value="Cập nhật" /><br />
                    </td>
                    <td id="TenCot" style="color:red;">
                        @Html.ActionLink("Xóa", "XoaGioHang", new { @__MaSach = item._MaSach })
                    </td>

                }
            </tr>
        }
        <tr>
            <td colspan="6" id="TenCot" style="color:green;">
                @Html.ActionLink("Trở lại Giỏ hàng", "GioHang")
            </td>
        </tr>
    </table>
</div>
